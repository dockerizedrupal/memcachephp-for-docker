memcachephp:
  image: viljaste/memcachephp:dev
  hostname: memcachephp
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - memcachephpdata
  environment:
    - SERVER_NAME=localhost
    - USERNAME=root
    - PASSWORD=root
memcachephpdata:
  image: viljaste/data:dev
  hostname: memcachephpdata
  volumes:
    - /memcachephp
