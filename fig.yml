memcachephp:
  image: simpledrupalcloud/memcachephp:dev
  hostname: memcachephp
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - memcachephpdata
  environment:
    - SERVER_NAME=localhost
memcachephpdata:
  image: simpledrupalcloud/data:dev
  hostname: memcachephpdata
  volumes:
    - /memcachephp/ssl/certs
    - /memcachephp/ssl/private
